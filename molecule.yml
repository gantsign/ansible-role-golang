---
docker:
  containers:

      - name: ansible-role-golang-01
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1

      - name: ansible-role-golang-02
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1