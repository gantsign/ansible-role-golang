---
ansible:
  playbook: tests/test.yml

docker:
  containers:
    - name: ansible-role-golang-ubuntu-trusty
      image: ubuntu
      image_version: '14.04'
    - name: ansible-role-golang-ubuntu-xenial
      image: ubuntu
      image_version: '16.04'
