---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

platforms:
  - name: ansible-role-golang-opensuse-leap
    image: opensuse:42.2

provisioner:
  name: ansible
  playbooks:
    converge: ../default/playbook.yml
  lint:
    name: ansible-lint

scenario:
  name: opensuse-leap

verifier:
  name: testinfra
  directory: ../default/tests
  lint:
    name: flake8
